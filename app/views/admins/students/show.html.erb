<%= link_to "topへ", admins_root_path %>
<div class="row">
  <div class="admin-user">
   <div class="col-md-8 col-md-offset-2">
     <div class="panel panel-default">
       <div class="panel-heading text-center">
         *プロフィール画像が入る
       </div>
       <div class="mannaka">
        <div class="panel-body">
         <%= @student.name %>
         <%= @student.email %>
         <%= @student.created_at %>
         <%= link_to "Delete", admins_student_path(@student), method: :delete, data: { confirm: "You sure?" }, class: 'btn btn-danger btn-xs' %>
       </div>
     </div>
   </div>
  </div>
 </div>
</div>
